<body >
  <div class="text-center py-5">
    <div class="container">
      <div class="row my-5 justify-content-center">
        <div class="col-md-9">
          <h1>案件一覧</h1>
            <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
            <a href="#" class="btn btn-primary m-2">マイポスト</a>
            <%= link_to("ブックマーク", favorites_index_path, {class: "btn btn-secondary m-2"}) %>
        </div>
      <div class="col-md-1２ ml-auto" style="">
        <form class="form-inline" style="">
          <div class="input-group">
            <input type="text" class="form-control" id="inlineFormInputGroup" placeholder="絞り込み検索">
            <div class="input-group-append"><button class="btn btn-primary" type="button"><i class="fa fa-search"></i></button></div>
          </div>
        </form>
      </div>
      </div>
    </div>
  </div>
  
  <div class="py-4 bg-light">
    <div class="container">
      <div class="row flex-row">
        <% @topics.each do |topic| %>
        <div class="col-md-4 p-3">
          <div class="card box-shadow">
            <img class="card-img-top">
            
            <% if topic.image.url %>
              <%= image_tag topic.image.url %>
            <% else %>
              <%= image_tag "/assets/default.png" %>
            <% end %>
              
            <div class="card-body">
              <p class="font-weight-bold"><%= topic.user.name %></p>
              <p class="font-weight-bold"><%= topic.shop.name %></p>
              <p class="card-text"><%= topic.description %></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <button type="button" class="btn btn-sm btn-outline-secondary"><%= link_to "View", topic %></button>
                </div> <small class="text-muted">9 mins</small>
              </div>
            </div>
          </div>
        </div>
     <% end %>
     </div>
    </div>
  </div>
  
  <footer class="text-muted py-5">
    <div class="container">
      <p class="float-right">
        <a href="#">Back to top</a>
      </p>
      <p>Album template is based on Bootstrap © examples.&nbsp; <br>New to Bootstrap? <a href="#">Visit the homepage</a> or read our <a href="#">getting started guide</a>.</p>
    </div>
  </footer>
 
</body>