<div class="py-5">
    <div class="container">
      <div class="row">
        <% @shops.each do |shop| %>
        <div class="col-md-12">
          <div class="list-group" >
            <li class="list-group-item d-flex justify-content-between align-items-center"><%= link_to shop.name, shop %>
              <%= link_to '編集', edit_shop_path(id: shop.id) , class: "btn btn-primary ml-auto" %>
              <%= link_to '削除', shop_path(id: shop.id),method: :delete , class: "btn btn-danger" %>
            </li>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>