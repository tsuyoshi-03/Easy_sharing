<div class="py-5">
    <div class="container">
      <div class="row">
        <% @users.each do |user| %>
        <div class="col-md-12">
          <div class="list-group" >
            <li class="list-group-item d-flex justify-content-between align-items-center"><%= link_to user.name, user %>
              <%= link_to '編集', edit_user_path(id: user.id) , class: "btn btn-primary ml-auto" %>
              <span class="btn btn-danger">削除</span>
            </li>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>